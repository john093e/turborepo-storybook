import { useContext } from 'react';
import {EditionContext} from '@contexts/app/specific/website/page-builder/edition';

interface Props {
  id: string;
}

const Component = ({ id }: Props) => {
//   const { layoutState, setSelectedComponentId } = useContext(EditionContext);
//   const handleClick = () => {
//     setSelectedComponentId(id);
//   };

//   return (
//     <div
//       className={`component ${
//         layoutState.selectedComponentId === id ? 'selected' : ''
//       }`}
//       onClick={handleClick}
//     >
//       {/* render the component content */}
//     </div>
//   );
};
export default Component;
